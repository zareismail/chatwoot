# Chatwoot Development Guidelines

The guidelines live in `AGENTS.md` so that every coding agent reads the same file. It is
imported below rather than duplicated — edit `AGENTS.md`, never this file.

@AGENTS.md
